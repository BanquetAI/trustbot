{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Inherits noUncheckedIndexedAccess: true from tsconfig.json
    // Do NOT override - production builds must match dev strictness
  },
  "exclude": [
    "node_modules",
    "dist",
    "web",
    "**/*.test.ts",
    "**/*.spec.ts"
  ]
}
