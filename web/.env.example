# Persistence Configuration
# Copy this to .env (local) or set in Vercel Project Settings

# --- Option A: Vercel Postgres (Recommended) --- 🐘
# Automatically set if you use "Vercel Storage" tab to create a Postgres DB.
POSTGRES_URL="postgres://default:password@ep-host.us-east-1.postgres.vercel-storage.com:5432/verceldb?sslmode=require"
STORAGE_PROVIDER="postgres"

# --- Option B: In-Memory (Demo Mode) --- 🧠
# Default if no variables are set. Resets on every deploy/restart.
# STORAGE_PROVIDER="memory"

# --- Option C: Vercel KV (Redis) --- 🗝️
# KV_REST_API_URL="..."
# KV_REST_API_TOKEN="..."
# STORAGE_PROVIDER="kv"

# --- Authentication (Optional) ---
# API_SECRET="your-secret-key-to-protect-endpoints"
